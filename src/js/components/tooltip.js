if (document.querySelector('.we-glad')) {
  const tooltip = tippy('[data-tippy-content]', {
    animation: 'scale',
    trigger: 'click',
    trigger: 'focusin',
    theme: 'sitdownpls',
    maxWidth: 250
  });
}
